# Flat Scramjet & Libcurl Proxy

A zero-folder, 7-file personal proxy built with Scramjet streams and native Libcurl bindings. Features automated silent background launching.

## Installation

Run the install command. The application will build its dependencies and automatically launch in the background.

```bash
npm install
